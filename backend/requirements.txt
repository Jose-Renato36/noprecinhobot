fastapi==0.118.0
uvicorn[standard]==0.37.0
sqlalchemy==2.0.43
pydantic==2.11.9
python-dotenv==1.1.1
curl_cffi==0.16.0
beautifulsoup4==4.14.2
lxml==6.0.2
apscheduler==3.11.0

# Testes. O TestClient do Starlette é construído sobre o httpx.
pytest==8.4.2
httpx==0.28.1

# Fallback de navegador para lojas que montam o preço por JavaScript.
# Depois de instalar, baixe o navegador uma vez:  playwright install chromium
# Sem esse passo o scraper segue funcionando só com HTTP — o fallback se
# desliga sozinho (ver app/navegador.py).
playwright==1.62.0
